import {Request, Response} from "express";
import db from "../helpers/db";

export const getAllBooking = (req: Request, res: Response) => {
    return;
}

export const bookingInfo = (req: Request, res: Response) => {
    return;
}

export const getMyBooking = (req: Request, res: Response) => {
    return;
}

export const createBooking = (req: Request, res: Response) => {
    return;
}

export const returnBooking = (req: Request, res: Response) => {
    return
}
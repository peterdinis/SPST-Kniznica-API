import { Request, Response } from "express";

export const myBorrowedBooks = (req: Request, res: Response) => {
    return;
}

export const createNewOrder = (req: Request, res: Response) => {
    return;
}

export const returnOrder = (req: Request, res: Response) => {
    return;
}
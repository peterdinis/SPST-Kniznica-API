import { Request, Response } from "express";
import db from "../helpers/db";

export const saveStudent = (req: Request, res: Response) => {
    return;
}

export const studentInfo = (req: Request, res: Response) => {
    return;
}

export const removeStudent = (req: Request, res: Response) => {
    return;
}